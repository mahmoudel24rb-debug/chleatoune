# MÉMOIRE PERSISTANTE DE CLAUDE CODE — export du 2026-07-03

> Ce fichier est l'export de la mémoire auto de Claude Code accumulée sur
> l'ancien PC pendant TOUT le développement (leçons, pièges, décisions,
> historique). **Instructions pour le Claude Code du nouveau PC** : à la
> première session, recopie le bloc ci-dessous dans ta propre mémoire
> persistante (un fichier `projet-birb-like-cadeau.md` dans ton dossier
> memory, + la ligne d'index dans MEMORY.md), puis appuie-toi dessus.
> Le contenu recoupe DOCUMENTATION.md mais contient en plus le contexte
> humain (qui, pourquoi, quelles décisions de l'auteur) — il fait foi
> pour « pourquoi c'est comme ça ».

---

---
name: projet-birb-like-cadeau
description: "Le jeu birb-like est un cadeau à personnaliser pour la copine de l'utilisateur"
metadata: 
  node_type: memory
  type: project
  originSessionId: eb9d82d0-48ee-46de-8dae-418320e78eb6
---

**REFONTE SWARM en cours** (plans de l'utilisateur dans `birb\plan refonte\`, 09→13, à lire avant de coder ; plan 12 = référence d'équilibrage, tout dans `src/data/swarm.ts`). **Plan 09 LIVRÉ** (2026-07-03) : modes monde/antre/donjon/peche, l'Antre (hub 13 portes, `data/portes.ts` positions+vagues scénarisées, `systems/antre.ts`), donjons à vagues (`systems/donjon.ts`, ex-combat.ts : budget de menace, spawn par paquets cap 30, pause 2,5 s, boss barre segmentée, élites halo, victoire=coffre+plumes 1ʳᵉ complétion+modal fin, K.O. clément, sans-fin record), save v3 (migration meilleurEtage→porteMax log-équivalence, ciseaux offerts, quêtes etage→donjon), sessionStorage `chleatoune_scene` pour reload→Antre. Test `test_plan09.py` 23/23. Casting GLB validé (voir `export/glb` + v2) : 12 boss thématiques (Maokai/Morgana/Skarner/Fizz/Bel'Veth/NunuBot/Ornn/Fiddle/Malphite/PoolPartyLeona/Nautilus/Vel'Koz + Aurelion Sol sans-fin), minions chaos = petits monstres, Kog'Maw=kamikaze, Kha'Zix=spectre, Mercier=Elderwood Bard, compagnons Sona/Choncc/Molediver/Hushtail. **REFONTE TERMINÉE (2026-07-03, commit 65ea7ea)** — plans 10/11/12/13 livrés en un bloc : bestiaire à 6 rôles (`data/monstres.ts`), télégraphes FIGÉS à la visée (`systems/telegraphes.ts`), pool 128 projectiles, 13 boss à patterns (`data/boss.ts` : charge/volée/pluie/invocation/anneau+enrage), grille spatiale `core/grille.ts`, 240 sprites GLB (24 modèles, sous-agent, outillage reproductible `outils/rendu-glb/bestiaire/` avec `facteurLimite` réglable — piège Kog'Maw : bombe posthume garée dans le mesh, limite 2,5), chargement paresseux `chargerSpritesGlb` à l'entrée du donjon. Mercier dans l'Antre (touche M, sprite Elderwood Bard) : 8 parchemins ×1,35 + 6 sorts autos niveaux 2-6 + 6 évolutions à 600 dorés ; ciseaux offerts à la 1ʳᵉ entrée dans l'Antre ; recouture n'efface jamais parchemins/sorts/évolutions/compagnons (vérifié E2E). Compagnons de biome : 5 espèces, adoption au panneau du biome (DOUGHCAT retiré de la boutique, migration max() dans save.ts), récolte statistique tick 5 s hors zone regardée, copies de combat à rôles, escouade max 3 cochée dans le modal de porte. Équilibrage : `saves-test/`×4 + `EQUILIBRAGE-JOURNAL.md` ; 2 corrections de clémence : **grâce 0,4 s après chaque coup** (SWARM.graceContactSec, sinon l'encerclement one-burst) et contact boss = glouton×2 (pas golem×2). Tests : `test_plan09.py` + `test_plans10-13.py` (scratchpad), pilote qui KITE en cercle (le pilote qui danse sur place meurt — normal). Guides à jour (public/guide.html + GUIDE-COMPLET-SPOILERS.html hors repo). Boss porte 6 un peu court (27 s) : si confirmé au playtest, monter pvBossBase 25→30. **Mode dev** : F1 → panneau « se donner des ressources » (monnaies/dorés/plumes/SP), mot de passe `RakTma123456` (`src/ui/dev.ts`), déverrouillage par session (sessionStorage `chleatoune_dev`). **CHANTIER v3+v4 TERMINÉ ET DÉPLOYÉ (2026-07-03)** — plans 14-18 tous livrés : plan 17 (pêche AC : `systems/peche.ts` machine à états vise/charge/attente/mordille/plongeon(0,75 s)/lutte/revele, ombres par espèce, `input.actionTenue()` pour maintenir ESPACE, `data/peche-config.ts`, `core/sprites-poissons.ts` rend `CARTES_POISSONS`, dex.tailleRecord additif, créneaux + ciel teinté, blagues [À ÉCRIRE] dans data/poissons.ts) et plan 18 (besace `save.inventaire`, `systems/besace.ts` + `consommables.ts` (hotbar 1/2/3, cd global 8 s, buffs câblés donjon/sorts via `cablerConsommables`), cuisine `data/cuisine.ts` chez Brioche, aquarium `save.aquarium` + vitrine rendue sur le ponton (F), vente auto jamais-shiny, pâtée/recyclage/offrande-poisson). PIÈGE RÉSOLU : `#game-area` est le CANVAS — tout élément DOM (dialogue, suivi, hotbar) doit s'appendre à `#game-wrap` (position:relative), jamais au canvas (enfants non rendus, rect 0×0). Tests : `test_v3v4.py` 47/47 + régressions plan09 et plans10-13 vertes. Guides à jour (spoilers : sections défis/malédictions/archi + fil rouge + pêche v4 + besace/aquarium ; sans-spoilers : pêche réécrite + « un monde qui murmure »). TEXTES TOUS ÉCRITS par Claude à la demande de l'utilisateur (commit 51e63f8, ton Dofus/Genshin) : dialogues 7 chapitres + ambiances, lettre de l'Atelier en VERSION FICTION signée « LE GRAND COUTURIER » (l'utilisateur peut la remplacer par une vraie lettre perso — le lui rappeler avant l'offrande finale), énigme du château « N'ENTRE PAS ARMÉE. ENTRE COUSUE. », 16 blagues de poissons, 8 commentaires de Brioche. RESTE côté utilisateur : éventuellement personnaliser lettre/blagues, et chronométrer « porte n+1 hotbar pleine » (journal). — (archive du plan de reprise : v3+v4 étaient les dossiers de plans `birb\v3\` et `birb\v4\`) : plans utilisateur dans `birb\v3\` (14 défis/malédictions/archimonstres, 15 fil rouge/PNJ/dialogues, 16 succès/secrets/chasses/calendrier) et `birb\v4\` (17 pêche Animal Crossing, 18 besace/cuisine/hotbar/aquarium). Consigne : TOUT implémenter, **UN SEUL push/deploy à la fin** (celui du 3e commit local ci-dessous N'A PAS eu lieu). FAIT & committé localement (build vert, 2 commits après 38b4cca) : plan 14 complet (défis HUD ✓/✗ ×1,4, malédictions cochables plafond ×2,5 cloisonné dorés/XP, archimonstres dorés 1,5 % + bestiaire UI relié au Mikudex + trophée Antre, dégâts taggés par source, `__jeu.debug.archiChance`) ; plan 16 complet (40 succès `data/succes.ts` + rattrapage au chargement, titres de rang + sélecteur dans profil, 15 fils secrets proximité <180 px, 10 chasses + Sphinge au désert (vend carte 120 ✦/indice 60 ✦) + flèche d'aide 90 s, calendrier 7 bonus jour appliqués partout + offrande, save additive : succes/titres/secrets/chasse/chassesFaites/calendrier/drapeaux/filRouge) ; plan 15 complet (L'ENVERS/LA DÉCHIRURE/LE MÉTIER renommés, boîte dialogue rétro `ui/dialogue.ts` (E avance, Échap coupe/reprend, monde gelé pendant), 4 PNJ sprites recyclés (brioche=doughcat, régisseuse=c_scene, yuumi, vieuxpic=c_mine) + marqueurs ❗/…, fil rouge 7 chapitres `data/filrouge.ts` (collecter = DELTA depuis acceptation via filRouge.compteur, cercle de scène 20 s ch2, forge ch4, chasse scénarisée ch5, Effilocheuse avant/après porte 12 ch6, Atelier ch7 = 3 croquis + lettre `THEME.lettreAtelier`), TOUS les textes persos en placeholders [À ÉCRIRE] à faire remplir par l'utilisateur). Sprites poissons AC-style LIVRÉS par sous-agent : `src/data/poissons-sprites.ts` (16 espèces ×2 frames, gabarit 32×20, hippocampe 16×28, planche validée — superbe). RESTE À FAIRE : **plan 17** (réécrire `systems/peche.ts` : ombres S/M/L/XL/AILERON, lancer chargé 3 bandes, mordillages/plongeon fenêtre 0,75 s, lutte capture/tension avec cannes ×tolérance, tailles cm + dex.tailleRecord (migration additive), créneaux horaires + ciel teinté, eau v2 + lanternes, pêcheurs visibles, `data/peche-config.ts` objet PECHE, sons plic/plouf/tension/casse DÉJÀ dans audio.ts, blagues par espèce champ `blague` [À ÉCRIRE], intégrer CARTES_POISSONS partout incl. Mikudex) ; **plan 18** (besace save.inventaire {poissons, plats}, capture→besace (XP tout de suite, valeur à la vente — vérifier quête 'pecher' branchée sur capture), vente par espèce/rareté/tout (jamais shiny), option vente auto, cuisine 7 recettes par paliers dex, hotbar 3 slots touches 1/2/3 + cd global 8 s save.swarm.hotbar, poissons crus croquables, Grand Aquarium save.aquarium (don consomme, shiny scintille, plaques tailleRecord, +2 plumes/4 dons, nage dans le bassin), pâtée du chat + recyclage appâts + offrande poisson) ; puis TESTS E2E (v3 : succès rafale/défi/malédiction ×2 exact/archi forcé/recouture intacte ; fil rouge profil neuf & avancé sans soft-lock ; v4 : critères des plans) + régressions plan09/10-13 + guides (spoilers + sans-spoilers) + EQUILIBRAGE-JOURNAL (porte n+1 hotbar pleine) + **UN SEUL git push final**. Piège existant à retenir : overlays↔donjon et sorts↔donjon sont des cycles d'import runtime-only VOULUS (ne pas « corriger »). **Le prestige s'appelle RECOUTURE** (féminin — « 1RE RECOUTURE », thème couturière de Gwen ; choisi par l'utilisateur, remplace « rebirb » ; les identifiants de code gardent rebirb). Le jeu est **déployé sur https://chleatoune.vercel.app** (cloud Upstash actif, guide sans spoilers sur /guide.html ; guide complet spoilers HORS repo dans `birb\GUIDE-COMPLET-SPOILERS.html`). **Cloud v2 façon MMO** (refonte après perte de données de l'utilisateur) : écran d'accueil CRÉER/SE CONNECTER, code de connexion `CHLEA-…` affiché à la création, enveloppe {nom, skin, maj, donnees} dans Redis, cycle 20 s **tirer-puis-pousser** avec version `maj` (clé locale `birblike_cloudmaj_<id>`) — un onglet ouvert ADOPTE la progression distante au lieu de l'écraser ; beacon de fermeture soumis au verrou de changement de perso (c'était LE bug : il poussait l'état de l'ancien perso sous le code du nouveau) ; gains hors-ligne appliqués APRÈS initCloud ; sw.js n'intercepte pas /api/. Test de référence : `test_v9.py` (2 contextes navigateur + API cloud simulée par page.route, rejoue le scénario de perte). Ne jamais jouer le même code sur 2 appareils actifs (dernier-poussé-gagne, convergence en ~20 s). Le projet `c:\Users\dglco\Documents\birb\birb-like` (jeu incrémental type Birb.io, Vite + TS + Canvas, construit le 2026-07-02 d'après les plans .md du dossier parent) est un **cadeau personnalisé pour la copine de l'utilisateur**. Thème appliqué le 2026-07-02 : le jeu s'appelle « Chléatoune », les popcorn sont remplacés par des Smiski (figurines vertes) ; l'id interne `popcorn` est conservé. L'héroïne est **Chibi Crystal Rose Gwen** (chibi rousse aux roses, TFT) : ses frames sont pré-rendues depuis `perso/chibi_crystal_rose_gwen.glb` via `birb-like/outils/rendu-glb/` (three.js headless + Playwright, matériaux unlit obligatoires pour les modèles LoL, clips `Idle_Base`/`Run_Base`, PNG 74×60 affichés ×2 depuis `public/assets/`). Sprites directionnels : 3 vues (face/dos/profil, `chleatoune_{vue}_{pose}.png`) choisies selon la direction du déplacement, profil retourné pour la gauche ; cadrage = union des bbox de toutes les poses **projetée dans chaque angle de vue** (sinon frames coupées — bug signalé deux fois par l'utilisateur ; penser aussi au cache navigateur : les URLs des PNG portent `?v=N`, incrémenter `VERSION_SPRITES` dans sprites.ts à chaque régénération). Système de combat ajouté (2026-07-02, inspiré des captures `donjons/` du Birb original) : zone DONJON (5e zone, toujours accessible), étages progressifs, boss tous les 5 étages, XP→niveaux→3 SP/niveau, compétences Vitalité/Récupération/Force (sous-onglet COMPÉTENCES), butin en smiski ; équilibrage dans `src/data/combat.ts`, logique dans `src/systems/combat.ts`. Ajouts ultérieurs le même jour : GRAINES → figurines MIKU (couleur #39c5bb, zone 2 « SCÈNE »), compagnons **doughcat** (glb `perso/doughcat.glb`, amélioration permanente `p_doughcat` ×4 max, auto-ramassage, `systems/compagnons.ts`), frames d'**attaque** de Gwen (clip `Cast_Cycle`, jouées via `birb.attaqueT`), hitbox au **centre du corps** (`centreBirb()`, la position x/y est aux pieds — piège récurrent). Leçon three.js : `Box3.setFromObject` ignore le skinning — utiliser `SkinnedMesh.computeBoundingBox()` pour cadrer des poses animées. Taille du perso : l'union des poses rapetisse le corps dans le cadre → `hauteur: 130` + `recadrer: 'vertical'` (ancrage au sol, élan horizontal conservé) dans generer.py donnent un corps ~54 px (×2 = ~108 px écran) ; l'utilisateur tient à cette taille. VERSION_SPRITES=4. **v2 « grosses maps »** (2026-07-02, d'après 14 captures du vrai Birb dans `export/`) : 3 modes de jeu (`core/mode.ts` : monde/expedition/peche), objets interactifs touche E (`systems/interactions.ts`, positions dans `systems/carte.ts`), expédition à 6 biomes avec coffres (portail du hall du donjon), arbre de talents en plumes (panneaux physiques prairie ; bonus passif = cumulPlumes, solde dépensable), désert doré (smiski dorés + arbre + marchand de quêtes `systems/quetes.ts`), autel de sacrifice (SP→+20 %/palier), pêche vue de côté + Mikudex 12 espèces (`data/poissons.ts`, ESPACE/F), arbre géant/nid → gains hors-ligne + Yuumi (glb `perso/chibi_yuumi.glb`) qui ramasse en forêt, message du château personnalisable (`THEME.messageChateau`). Sauvegarde version 2 avec migration. Tests : suites `test_v4/v5/v6.py` pilotent l'héroïne via `window.__jeu` ; rayons d'interaction ≥ 80 px sinon le pilote rate les panneaux. **Pêche v2** (specs de la copine) : déplacement sur le ponton + banc de poissons mobile (bonus), boutique touche B (`data/peche-boutique.ts` : 4 cannes, 3 appâts consommables, 3 pêcheurs auto qui pêchent partout, 1 prise/40 s), 16 espèces 4 raretés. UI : barres niveau/PV seulement en expédition ; doughcats combattent (25 % des stats via `PART_STATS_CHAT`, respawn 30+0,5×niveau s). Port dev FIXÉ à 5199 (`vite.config.ts` strictPort) car localStorage lié à l'origine — l'utilisateur a « perdu » une sauvegarde à cause d'un changement de port (récupérable sur l'ancien port via export/import). **Déploiement choisi : web sur Vercel + PWA** (copine sur MacBook) — manifest/sw.js/icônes prêts, `DEPLOIEMENT.md` documente `npx vercel --prod` + intégration Upstash Redis pour la **sauvegarde cloud** (`api/sauvegarde.js` + `systems/cloud.ts`, code `CHLEA-…` par personnage, dégradation silencieuse hors déploiement). **Créateur de personnage** : écran « QUI JOUE ? » (`ui/creation.ts`), profils multi-sauvegardes (`systems/profils.ts`, clé `birblike_save_<id>`, migration auto de l'ancienne clé), **7 skins** (`data/skins.ts` : chleatoune/gwen/cafecuties/soulfighter + sett/heartsteel/spiritblossom — les Sett frappent aux poings, clip `Cast_Animation` t=(0.4, 0.9)). Le cadrage glb utilise désormais la **médiane du nuage de sommets** (rayon = 8× distance médiane) : c'est ce qui a réparé Soul Fighter et Heartsteel dont les accessoires garés cassaient même la bind pose. Repo GitHub : `mahmoudel24rb-debug/chleatoune` (branche main, birb-like à la racine, `git push` = redéploiement Vercel une fois lié ; gh CLI authentifié sur ce compte). Piège corrigé : verrouillerSauvegarde() avant un changement de profil sinon beforeunload écrit l'état de l'ancien perso sous la clé du nouveau. Leçons pipeline glb : clips `Run` vs `Run_Base` selon l'export ; `frustumCulled=false` obligatoire ; filtrer les sommets à >3× la diagonale bind (accessoires cachés à ~10⁴ unités). **Electron : INTERROMPU à la demande de l'utilisateur avant l'empaquetage** — la plomberie est en place et inerte (electron/main.cjs + preload.cjs, `apiBureau` dans save.ts, boutons fichier dans le menu, icône build/icon.ico, deps installées, `base:'./'` + chemins d'assets relatifs) ; l'exe se ferait avec `npm run dist:app`. Ne pas relancer l'empaquetage sans son accord. Toute la personnalisation passe par `src/data/config.ts` (THEME/ZONES) et `src/core/sprites.ts` (pixel maps) — guide dans `PERSONNALISATION.md`. Le plan 08 (multijoueur) n'est pas implémenté. Attention : un autre serveur dev occupe souvent le port 5173 sur cette machine (dashboard crypto) — lancer Vite avec `--port 5199 --strictPort` pour tester.
